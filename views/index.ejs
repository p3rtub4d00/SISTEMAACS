<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema ACS - Famílias</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="navbar navbar-dark bg-primary mb-4 no-print">
        <div class="container">
            <a class="navbar-brand" href="/">ACS Digital - Gestão de Famílias</a>
            <a href="/cadastro" class="btn btn-light text-primary fw-bold">+ Nova Família</a>
        </div>
    </nav>

    <div class="container">
        <form action="/" method="GET" class="mb-4 no-print">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Buscar por nome da família ou paciente...">
                <button class="btn btn-primary" type="submit">Buscar</button>
            </div>
        </form>

        <h3 class="mb-3">Famílias/Pacientes Cadastrados</h3>
        
        <% if(pacientes.length > 0) { %>
            <div class="list-group">
                <% patients = pacientes %>
                <% pacientes.forEach(function(paciente) { %>
                    <a href="/paciente/<%= paciente._id %>" class="list-group-item list-group-item-action d-flex justify-content-between align-items-center">
                        <div>
                            <h5 class="mb-1"><%= paciente.nome %></h5>
                            <small class="text-muted">CNS: <%= paciente.cns %> | Microárea: <%= paciente.microarea %></small>
                        </div>
                        <span class="badge bg-secondary rounded-pill">Ver / Imprimir</span>
                    </a>
                <% }); %>
            </div>
        <% } else { %>
            <div class="alert alert-info">Nenhum cadastro encontrado.</div>
        <% } %>
    </div>
</body>
</html>
