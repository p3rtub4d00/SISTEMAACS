<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema ACS - e-SUS APS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <nav class="navbar navbar-dark bg-primary mb-4 no-print">
        <div class="container">
            <a class="navbar-brand" href="/">ACS Digital - e-SUS</a>
            <a href="/cadastro" class="btn btn-light text-primary fw-bold">+ Nova Ficha</a>
        </div>
    </nav>

    <div class="container">
        <form action="/" method="GET" class="mb-4 no-print">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Buscar por Nome, CPF ou CNS...">
                <button class="btn btn-primary" type="submit">Buscar</button>
            </div>
        </form>

        <h3 class="mb-3">Cidadãos Cadastrados</h3>
        
        <% if(pacientes && pacientes.length > 0) { %>
            <div class="list-group">
                <% pacientes.forEach(function(paciente) { %>
                    <a href="/paciente/<%= paciente.id %>" class="list-group-item list-group-item-action">
                        <div class="d-flex w-100 justify-content-between">
                            <h5 class="mb-1 fw-bold"><%= paciente.nome %></h5>
                            <small class="text-muted">Microárea: <%= paciente.microarea %></small>
                        </div>
                        <p class="mb-1">CNS: <%= paciente.cns || 'Não informado' %> | CPF: <%= paciente.cpf || 'Não informado' %></p>
                        <small class="text-primary fw-bold">Ver Ficha Completa →</small>
                    </a>
                <% }); %>
            </div>
        <% } else { %>
            <div class="alert alert-warning text-center">
                <h4>Nenhuma ficha encontrada.</h4>
                <p>Clique em <strong>+ Nova Ficha</strong> para começar.</p>
            </div>
        <% } %>
    </div>
</body>
</html>
